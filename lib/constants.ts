export const CAPITAL_REGION_TAG = "Capital Region, NY";

/**
 * Update this date each time the catalog in lib/regional-data.ts is
 * reviewed/refreshed. The site commits to reviewing every listing at least
 * every 2 weeks (see the note surfaced near the catalog on Explore and the
 * landing page) — bump this whenever that review happens, even if no
 * records changed.
 */
export const CATALOG_LAST_REVIEWED = "2026-09-01";
export const CATALOG_REVIEW_CADENCE_DAYS = 14;
